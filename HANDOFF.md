# 이어서 할 일 (2026-08-08 밤 기준)

현재 버전 **1.10.519**, 테스트 62개 통과, 모두 푸시됨.

## 1. 운영자가 해야 할 것 — 서버 배포

```bash
cd "/Users/gimminhyeon/Documents/Codex/2026-06-09/new-chat-2/badminton" && firebase deploy --only functions
```

**아직 안 했습니다.** 이걸 안 하면 오늘 만든 선수 교체(`official-active-replace`)가
전부 "지원하지 않는 요청"으로 거절됩니다. 지난번 미배포분(경기 취소 후 자동 투입
제외)도 같이 올라갑니다.

## 2. 답을 기다리는 질문 — 선수 교체의 범위

오늘 만든 것은 두 가지입니다.

| 방식 | 동작 |
|---|---|
| 대기 선수 투입 | 뛰던 선수가 빠지고 쉬던 선수가 들어옴. 한 코트만 바뀜 |
| 코트 간 맞교환 | 1코트 A ↔ 2코트 B 가 서로 코트를 바꿈. 둘 다 계속 경기중 |

**아직 없는 것: 같은 코트 안에서 팀만 바꾸기.**
`가·다 vs 나·라` → `가·나 vs 다·라` 처럼 같은 4명으로 편만 재구성하는 것.

운영자에게 확인 중이던 것:
- 현장 요청이 어느 쪽이었나 (코트 간 교환인가, 같은 코트 팀 재구성인가)
- 코트 간 맞교환이 실제로 쓰이는가. 안 쓰면 후보 목록에서 빼는 게 나음
  — 목록이 길면 급할 때 오조작이 납니다

## 3. 남은 큰 것 — 5단계 (근본 원인)

**오늘 밤 실제로 재발했습니다.** 서버 rev 21, 관리자 화면은 낮 12시에 멈춤,
명령 기록은 4건만 남아 복구 불가.

원인: 관리자 화면이 세션을 **통째로 게시**하는데, 그 게시는 서버 리비전을 순서대로
따라잡아야 합니다. 명령 기록은 60분만 보관되므로 한 시간 이상 어긋나면 영원히
복구되지 않습니다.

응급 처치로 `dailyAdoptServerState()`(상황판 상단 「서버 기준으로 다시 맞추기」
배너)를 만들었지만, **덫 자체는 그대로입니다.** 5단계
(`_dailyWriteCheckinPayload` 제거 = 관리자 직접 쓰기 폐지)를 해야 재발이 없습니다.

## 4. 그 밖에 미결

- **`database.rules.json`** — 조인 규칙을 만들어 뒀으나 배포 보류(운영자 판단).
  `firebase deploy --only database` 로 적용. 반쪽짜리라 급하지 않음
- **실측 미완료** — 도착 전 등록(모달), 수동 경기 등록, 다음 대진 투입,
  대진 선수/순서 변경

## 오늘 배운 것 (반복하지 말 것)

- **화면에서 실제로 실행해 확인할 것.** 오늘 만든 교체 기능은 같은 이름의 옛
  함수가 파일 뒤쪽에 남아 새 함수를 덮고 있어 **한 시간 동안 실행조차 안 됐습니다.**
  단위 테스트는 통과했습니다
- **서버·클라이언트에 같은 검사가 두 벌이면 한쪽만 고치지 말 것**
- **실기기 확인은 로컬 HTTPS 리그 말고 실제 배포로**
