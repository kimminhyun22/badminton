# 이어서 할 일 (2026-08-11 기준)

버전 **1.10.557**, 테스트 64개 통과, **서버 함수 배포 완료**(538~553은 클라이언트만, 554에서 서버 재배포).
주말~화요일에 나간 것 — 1.10.521(교체 경로 수리) · 522(참가 등록
통합) · 523(임원 교체 시트) · 524(시트 디자인 수리) · 525(다음 대진 교체 개방) ·
526(다음 대진 짜기·순서 변경·후보 무제한) · 527(수동 편성 전면 교착 수리 —
28명 사전 시뮬레이션이 발견) · 528(첫 실전 피드백 6건) · 533~536(명부 클럽
라벨·부분 동기화·오늘 클럽만) · 537(세션 클럽 기록) · 538~540(역순 등록 흐름) ·
541~542(관리자 중복 메뉴 정리 → 대시보드 중심 재편) · 543(복구 배너 닫기 +
오늘 클럽 지정) · 544(임원 화면 버튼 위계) · **545(명부 오클럽 진짜 원인 수리 —
임원 화면이 이름순 정렬로 클럽 순서를 버리고 있었다)** · 546~553(임원 화면
간소화: 아이콘 줄·운영현황 도구 통합·이름 확대·군더더기 제거·직접 예약 딱지).

## 실전 피드백 반영 (2026-08-10 밤, v1.10.528)

첫 실전(28명)에서 나온 6건:

1. **임원 설정 풀림(버그)** — `applyPlayerCreate` 가 `isClubOfficial:false` 를
   하드코딩. **도착 전 일괄 등록으로 들어간 임원은 전부 자격을 잃었고**, 현장에서
   이름을 골라도 임원 인식이 안 됐다(도우미로 강등 운영). 명부 후보 경로
   (`applyPlayerAdd`)는 보존해서 "일부만" 풀림. 서버·전송 payload·재생 3곳 수리,
   회귀 고정. **다음 세션에 도착 전 등록을 다시 쓰면 정상**
2. **임원 대시보드** — 대시보드(운영 현황 8카드·명단·도구)는 이미 있었고,
   1번 버그로 임원 인식이 안 돼 안 보였던 것. 별도 코드 변경 없음
3. **편성 시 예약현황** — 다음 대진 짜기 시트 상단에 「현재 다음 대진」
   (순위·구성·경기중 보류 표시). 새 대진이 몇 순위로 붙는지 예고
4. **마무리 임원 개방** — `official-finish-mode` adminOnly 제외, 임원 화면에
   「마무리 전환/해제」 버튼(클럽 임원 전용, 도우미 제외)
5. **지각자 순번 안내** — 참가 등록 후 토스트: "처음 2경기 우선 편성 — 지금
   대기 N개 뒤 새 대진부터 앞순위". 실제 순번은 보충 시점에 정해지므로 규칙을
   안내(회원들이 임원에게 묻는 질문)
6. **편성 순번 안내** — 확인창·완료 토스트가 "N순위로 추가 · ○○ 경기 종료 후
   투입"을 말함
7. **(v1.10.529) 상태 표기 직관화** — 「후 휴식/후 종료」 → 「경기 후 휴식/경기
   후 종료」(임원·관리자 양쪽), 경기중 라벨 옆에 깜빡이는 빨간불(`live-dot`,
   prefers-reduced-motion 존중). 본인 카드 칩에도 동일. 참고: 본인 상태 버튼은
   이미 "경기 후 휴식"이었고 운영 현황 목록만 줄임말이었음 — 시각 위계 회귀가
   줄임말 복귀를 잡음

## 관리자 동일 기능 + 자격 복구 (v1.10.530, 서버 배포 완료)

운영자 추가 지시 둘: "진행 중인 게임 보는데 임원 권한 처리 안 된 것 같아" ·
"임원 대시보드 — 관리자와 동일한 기능 제공".

**임원 권한이 여전히 안 되는 이유** — v528 수리는 새로 만드는 선수부터 적용.
**이미 만들어진 세션의 선수 데이터에는 풀린 자격이 굳어 있어** 엔진 수리로는
낫지 않는다. 그래서:

- **`official-player-official` 신설(관리자 전용)** — 세션 선수의 임원 자격
  부여/해제. 자격 부여는 보안 경계라 유일하게 관리자 전용으로 남는 명령
- **관리자 상황판 복구 배너** — 명부에 임원인데 세션에서 임원이 아닌 선수를
  자동 감지해 「임원 자격 복구 (N명)」 한 번에 전송(`dailyRestoreOfficialFlags`).
  실측: 풀린 임원만 정확히 감지, 정상 임원·일반 회원 미포함

**관리자 동일 기능** — `adminOnlyCommand` 를 자격 명령 하나로 줄였다(전면 개방).
임원 화면(클럽 임원 전용, 도우미 제외)에 추가된 UI:

| 기능 | 위치 |
|---|---|
| 경기 취소 | 진행 코트 카드 (경기 종료 옆, 붉은 버튼) |
| 대기 다시 짜기·삭제 | 대기 카드 버튼 줄 |
| 선수 추가(게스트 포함) | 운영 패널 폼(이름·급수·성별·게스트) |
| 이름 변경·제외 | 운영 현황 도구 모드 2종 추가 |
| 코트 수 변경 | 운영 패널 버튼 |
| 마무리 전환/해제 | v528에서 이미 추가 |

실측: 7종 명령 전송 payload 전부 캡처 확인, 도우미에게는 전부 비노출 확인.
낡은 테스트 4개(관리자 전용 시절 가정)를 운영자 결정 기준으로 뒤집었다.

## 대기 팀 보호 — 해체 대신 자동 보충 (v1.10.531, 서버 배포 완료)

> 운영자 2026-08-11: "대기 팀을 해체하지 않고 1명을 자동 투입하는 게 공정"

수동 편성·수동 경기 등록이 대기 대진의 선수를 데려갈 때, 종전에는 그 대진을
**통째로 해체**해 남은 3명이 순번을 잃었다. 이제 `extractPlayersFromPrepared` 가
**팀과 순번을 그대로 두고 빈 자리만 자동 보충**한다(균형 점수 최적, 수동
편성이면 덜 뛴 순 폴백). 보충할 대기 인원이 없는 대진만 해체. 편성 확인창이
"○○ 선수의 원래 대기 팀은 순번을 지킨 채 빈 자리가 자동 보충됩니다"를 안내.
회귀 1c가 순번 유지·3명 보존·이중 배치 금지를 고정. 참고: 다음 대진 짜기는
**맨 뒤 순위**로 붙는다 — 우선 투입하려면 ▲위로.

## 도착 전 등록 스냅샷 갇힘 + 명부 불러오기 (v1.10.532)

- **"참가 등록 준비 중" 갇힘 수리** — 임원의 도착 전 참가 등록은
  `officialArrivalPlayers()` 후보에 의존하는데, 이 함수가 관리자 게시 시점의
  `arrivalCandidates` 스냅샷만 봤다. **게시 후 서버 명령으로 등록된 도착 전
  선수는 스냅샷에 없어 등록 불가.** 이제 명단의 planned·invited 선수를 항상
  포함하고(스냅샷 기능 꺼진 낡은 세션 포함), 스냅샷의 명부 후보와 병합·중복
  제거. 실제 함수를 실행하는 회귀로 고정
- **명부 불러오기** — 임원 선수 추가 폼 위 「명부에서 불러오기」 버튼 →
  명부 후보(급수·성별)와 도착 전 선수 시트 → 누르면 참가 등록 흐름.
  직접 입력에 **명부에 있는 이름을 타이핑하면 자동으로 명부 프로필 등록**으로
  라우팅(게스트 체크 시엔 동명이인 게스트 생성). 명부 프로필이 들어와야
  급수·성별·임원 표시가 유지된다 — 임원 자격 풀림 사고의 예방책이기도 함

## 명부 클럽 오매칭 수리 (v1.10.533)

> 실전 2026-08-11: "일만클럽 명부가 나와야 하는데 미르클럽 명부가 나왔어"

원인: 관리자가 게시하는 명부 후보(`_dailyOfficialArrivalCandidates`)가 **클럽
하나를 겹침 점수로 추정**해 실었다. 이름이 겹치는 클럽이 여럿이면 추정이
뒤집힌다. **격리 대신 라벨**로 바꿨다: 모든 클럽을 클럽명과 함께 싣고, 오늘
명단과 겹침이 큰 클럽이 앞, 같은 이름은 앞선 클럽 프로필만. 임원 시트는
「도착 전 / 일만클럽 / 미르클럽」 클럽별 그룹으로, 지각 등록 셀렉트도 클럽명
표시. 옛 단일 클럽 격리를 고정하던 회귀 2개(club-official·roster-isolation)를
새 계약으로 갱신 — 격리의 원래 목적(타 클럽 오인 방지)은 라벨이 대신한다.
**주의: 후보는 관리자 게시에 실리므로, 관리자 화면이 v533 으로 한 번
저장·게시해야 임원 화면에 반영된다.**

## 후보 자동 동기화 + 타 클럽 접기 (v1.10.534~535)

- **(534) 후보 부분 동기화** — 위 "저장·게시해야" 안내가 실전에서 실패했다:
  쓰기 통일 이후 라이브 중 관리자 조작은 명령 경로만 돌아 **전체 게시가 아예
  없다.** 휴식 전환을 해도 후보는 낡은 채였다. `_dailySyncArrivalCandidates` 가
  일시정지 방식의 **부분 쓰기**(`/session/arrivalCandidates`)로 후보만 따로
  맞춘다 — 관리자 화면 소유 확인 직후 + 명단 저장 브리지에서, 해시 같으면 생략,
  전체 게시 성공 시 해시 표시. 이제 관리자·임원 새로고침만으로 반영된다
- **(535) 타 클럽 접기** — "모든 클럽 명부 다 나옴": 접힌 줄로 축소했으나
- **(536) 오늘 클럽만** — 운영자 재확인 "일만 세션이니까 일만만": 임원 화면의
  명부 시트·지각 등록 셀렉트에서 **오늘 클럽만** 노출(접힌 줄도 제거).
  오늘 클럽 = 후보 순서의 첫 클럽(관리자 게시가 겹침 큰 클럽을 앞에 둠).
  타 클럽 방문자는 게스트 폼으로 등록 — 단, **이름 타이핑 자동 라우팅은 전
  클럽 프로필을 찾으므로** 타 클럽 회원 이름을 치면 그 프로필(급수)로 들어간다.
  게시 데이터에는 여전히 전 클럽이 실려 있다(오늘 클럽 오판 시 진단·복구용)

## 세션 클럽 기록 (v1.10.537, 배포·푸시 완료)

운영자가 원인과 해법을 함께 짚음: "오늘 미르클럽 회원이 많기 때문 … 최초 선수
등록 시 불러온 명부를 그대로 따라가면 되잖아". 겹침 점수는 미르 회원이 정말
많이 뛴 날 정직하게 — 그리고 틀리게 — 미르를 골랐다. **추정 대신 기록**:
관리자가 처음 선수를 등록한 명부 탭의 클럽을 `_dailySessionClubName` 으로
기록하고, 일일 상태에 저장(`sessionClub`)·복원, 초기화·준비 복원 시 비움.
`_dailyOfficialArrivalRoster` 는 기록이 있으면 그 클럽, 없을 때만 겹침 폴백.
**주의: 이미 살아있는 세션에는 기록이 없다** — 관리자 새로고침 후 오늘 클럽
탭에서 1명 등록하면 그때 기록된다(다음 세션부터는 자동).

## 역순 등록 흐름 (v1.10.538) — 전원 현장, 안 온 선수만 도착 전

> 운영자 2026-08-11: "사전등록 시 참가 등록하면 전원 현장으로 처리하고
> 현장에서 도착 전 선수만 체킹하고 바로 대진 게시" · "기본 현장으로 처리하고
> 도착 전인 경우만 별도 처리하는 게 간편해"

기존 흐름(전원 도착 전 → 당일 일괄 도착 확인)을 뒤집었다. 이제 표준 운영은:

1. **사전**: 참가자 모달에서 전원 「현장 참가 등록」(기본값이 현장)
2. **당일**: 모달을 다시 열면 참가 중 선수가 초록 「참가 중」 태그로 뜨고
   다시 선택 가능 — **안 온 선수만 체크** → 「도착 전 등록」이 그 선수들만
   planned 로 되돌림 → 바로 대진 게시
3. 늦게 도착하면 임원 화면에서 참가 등록(기존 그대로)

구현 요점:

- `renderDailyImportMembers` 의 `canRevert`: **게시 전**(`!_dailyCheckinId`) +
  wait + 경기중 아님 + 0경기만 되돌리기 후보. 게시 후에는 planned 전환 서버
  명령이 없으므로 잠긴다(기존 모달 흐름은 그대로 동작)
- `importDailySelected` 의 `revertToPlanned`: `dailySetStatus` 의 정리 경로를
  그대로 — 게임신청 취소(`_dailyCancelReservationsForPlayer`) + 대기표 교체/
  제거 후 planned 적용, `preArrivalVisible=true`, 대진 미리보기 무효화
- 명부 밖 선수(게스트) 행도 같은 되돌리기 대상. 모달 안내 문구·머리글
  라벨(「선택 가능」) 갱신, "현장 참가으로" 조사 오류도 수리
- 회귀: bulk-arrival 테스트에 vm 실행 섹션 추가(되돌리기·대기표 정리·뛴 선수
  보호·게시 전 한정·안내 문구). 브라우저 리그로 전 구간 실측 완료
- **(539) 처음 등록 정리** — 운영자 "처음 선수등록할 때 도착 전 등록 버튼
  정리": 게시 전 + 되돌릴 참가 중 선수 없음(= 처음 등록)이면 하단 「도착 전
  등록」과 게스트 「도착 전으로 추가」를 숨긴다(`showPlanned`). 당일 재오픈
  (되돌릴 선수 있음)·게시 후(지각자 서버 등록 경로)에는 그대로 노출. 게스트
  이름 Enter 기본값도 planned→wait. 세 상태 모두 리그 실측
- **(540) 기존 참가자 행 제거** — 운영자 "기존 참가자 명부는 다시 보여줄 필요
  없는 거 아냐": 선택해도 할 게 없는 회색 「이미 참가 등록」 행을 목록에서
  아예 뺐다(머리글 「이미 있음 N명」 숫자로만 남음). **되돌리기 후보인 초록
  「참가 중」 행은 남는다** — 안 온 선수 체킹 수단이라 빼면 역순 흐름이 죽음.
  게시 후 전원 등록이면 「전원 이미 등록되어 있습니다」 한 줄. semi-auto
  회귀의 isDup 고정 규칙을 새 계약(이전 체크만 보존)으로 갱신

## 관리자 중복 메뉴 정리 (v1.10.541)

운영자: "공용 링크·운영기록 링크 공유 모두 같은 거 아냐?" — 맞음. 세 버튼이
전부 `dailyShareCheckinLink()` 하나였다. 정리 결과:

- **링크 공유 2곳으로**: 상황판 퀵 「링크 공유」 + 공용 링크 카드(긴 이름
  「회원·임원 공용 링크 공유」 → **「링크 공유」로 축약**). 운영 기록 카드의
  중복 버튼은 제거
- **초기화 1곳으로**: 상황판 퀵 「초기화」만. 공용 링크 카드의 「데이터
  초기화」(같은 `dailyReset()`)는 제거
- 비(非)민턴LIVE 구역의 ×2 핸들러(저장·내보내기·인쇄 등)는 데스크톱/모바일
  반응형 쌍이라 중복이 아님 — 건드리지 않음
- official-link 회귀가 진입점 수를 고정: 링크 공유 2 · 초기화 1

## 대시보드 중심 재편 (v1.10.542) — 공용 링크 카드 은퇴

운영자: "공용 링크 영역 없애고 상단 링크 공유·링크종료·초기화로. 대시보드
중심으로 한눈에" + "대시보드는 기존처럼 버튼형, 클릭하면 해당 이슈 처리".

- **상황판 퀵 액션 세 개**: 링크 공유(파랑)·링크 종료(노랑, 자체 확인창)·
  초기화(빨강). `dailyCheckinDetails` 카드 삭제, 진입점은 각 1개(회귀 고정 —
  541의 "공유 2곳" 계약을 대체)
- **확인 필요 요청 상자는 상황판 안으로**: `dailyCheckinBox`가 중요 알림 아래로
  이동, **요청이 있을 때만** 「확인 필요한 요청 N건」으로 나타나고 행마다
  반영/무시 버튼으로 즉시 처리(버튼형 원칙). 링크 주소 표시는 「링크 공유」가,
  뒷풀이 블록은 상황판 스포트라이트가 대신(둘 다 카드 안 중복이었음)
- 카드 요약 `(N건 확인 필요)`·"준비가 끝나면…" 안내·"확인 필요한 요청 없음"
  채움말 모두 은퇴. 알림 점프 request 타깃은 `dailyCheckinBox`로 갱신
- **빈 노란 막대 수리**: `.daily-reconcile-banner`에 `[hidden]` 가드가 없어
  숨겨도 빈 테두리가 보였음(다른 배너는 전부 있었음) — 가드 추가·회귀 고정
- 옛 카드를 고정하던 회귀 2건(브랜드 표기 목록·뒷풀이 관리자 표시)을 새
  계약으로 갱신. 운영 타일(진행/대진/라이브)은 버튼형 그대로

## 파트너 반복 수리 (v1.10.557, **서버 배포 완료**) — 운영자 승인

> 운영자 2026-08-13: "파트너 반복을 줄여줘. 컴플레인이 몇 번 있었어" → 거래
> 설명 후 "그렇게 해"

**인당 반복률**(전체 경기 중 '이미 뛴 파트너'와 다시 뛴 경기 비율,
`HEADCOUNT=32 node tests/sim-repetition.js 4 12 mixed`):

| 조건 | 전 | 후 | 빈 코트 |
|---|---|---|---|
| 32명·4코트 | **53.1%** (3.19회/6경기) | **7.3%** (0.44회) | 0회 |
| 32명·3코트 | 2.8% | 2.8% | 0회 |
| 32명·5코트 | 46.7% | 13.3% | 0회 |
| 25명·3코트 | 37.5% | 15.3% | 0회 |
| 25명·4코트 | 38.5% | 11.5% | 0회 |
| 25명·5코트 | 30.0% | 43.3% | 0회 |

**원인은 점수가 아니라 대기표 깊이였다.** 목표 = 코트 수라 32명·4코트면 진행
16 + 대기 16 = **전원이 묶인다.** 한 경기가 끝나면 남는 사람은 방금 같이 끝난
그 넷뿐이라 **그대로 다시 묶였다** — 선택지가 0이면 어떤 가중치도 무의미하다.

**고친 것 둘:**

1. `desiredNextTarget` — 대기표가 대기 인원을 통째로 삼키는 **포화 상태에서만**
   목표를 한 경기분 줄인다(`raw <= official && raw >= 2 && 접수된 신청 없음`).
   접수된 게임신청이 있으면 건드리지 않는다 — 신청한 짝의 자리를 뺏지 않도록
2. `PENDING_PAIRS` — 아직 안 끝난 조합(진행 중·대기표)도 반복 페널티에 포함.
   자동 편성 중에만 켜고 `finally` 로 끈다(세션 저장 오염 방지)

**감수한 것**: 회원·관리자 화면에 보이는 다음 대진이 포화 상태에서 하나 줄어든다.
계약 2개를 새 기준으로 갱신 — `daily-server-replenish-regression`(목표는 코트
수 '이하'), `daily-admin-operation-command-regression`(순서 이동 픽스처를
인원 16·코트 3으로 넓힘).

**실전 조합 실측**(운영자 2026-08-13 "인원 적으면 코트를 줄이니 일반적인
경우만 보면 된다" — 아래가 그 범위):

| 구성 | 반복률 | 1인 평균 | 빈 코트 |
|---|---|---|---|
| 16명·2코트 | 22.9% | 1.38회/6.0경기 | 0회 |
| 20명·3코트 | 19.4% | 1.40회/7.2경기 | 0회 |
| 24명·3코트 | 6.9% | 0.42회/6.0경기 | 0회 |
| 25명·3코트 | 15.3% | 0.88회/5.8경기 | 0회 |
| 28명·3코트 | 8.3% | 0.43회/5.1경기 | 0회 |
| 28명·4코트 | 9.4% | 0.64회/6.9경기 | 0회 |
| 30명·4코트 | 7.3% | 0.47회/6.4경기 | 0회 |
| 32명·4코트 | 7.3% | 0.44회/6.0경기 | 0회 |
| 36명·4코트 | 5.2% | 0.28회/5.3경기 | 0회 |
| 36명·5코트 | 7.5% | 0.50회/6.7경기 | 0회 |
| 40명·5코트 | 7.5% | 0.45회/6.0경기 | 0회 |

**24명 이상은 5~9%로 안정.** 16~20명이 20% 안팎인 것은 수리 실패가 아니라
**모수가 작아 생기는 하한**이다(16명이 6경기씩 뛰면 무작위로 섞어도 그 정도).
빈 코트는 전 구간 0회.

**구조적 한계(실전에는 없는 배치)**: 25명·5코트처럼 대기 인원이 얇으면
(20명이 코트에, 대기 5명) `raw < 2` 라 여유가 안 들어가고 반복이 오히려
늘 수 있다(43.3%). 운영자 확인: 인원이 적으면 코트를 줄여 진행하므로
실전에서는 나오지 않는 배치다.

## 중복 파트너·상대 실측 (v1.10.555) — 컴플레인은 근거 있음

> 운영자 2026-08-13: "중복 상대 및 파트너 게임이 많다는 컴플레인이 있는데
> 어느 정도인지 적정한지 여부 알고 싶어"

`tests/sim-repetition.js` 로 실측(개입 없는 순수 자동 운영, 15분×12파동=180분).
명부 원본은 관리자 브라우저에만 있어 **실분포 비율(남20/여12, S1 A5 B6 C9 D10
E1)을 유지한 합성 명부**를 씀. `HEADCOUNT=25 node tests/sim-repetition.js 4 12 mixed`
형태로 재현.

| 조건 | 1인당 또 만난 파트너 | 무작위 기준선 | 1인당 또 만난 상대 | 기준선 |
|---|---|---|---|---|
| 32명·4코트 | **2.06~2.50명** | 0.48 | 2.88~3.00명 | 2.13 |
| 32명·5코트 | 2.63명 | 0.79 | 3.00명 | 3.39 |
| 25명·3코트 | 1.68명 | 0.57 | 2.80명 | 2.52 |
| 25명·4코트 | 1.92명 | 1.07 | 3.92명 | 4.60 |

- **파트너 중복은 과다**: 32명에서 무작위의 **4~5배**. 6경기 뛰면 2~3번은 또
  같은 파트너. 25명에서는 2~3배로 완화 — 인원이 많을수록 나빠진다
- **상대 중복은 정상 범위**: 대체로 기준선과 비슷하거나 오히려 적다. 상대는
  한 경기에 3명이라 체감이 클 뿐
- **원인(코드 확인)**: 반복 페널티는 있다(파트너 1회 240 · 2회 1400 · 3회
  사실상 금지). 그런데 ① 공정성 항이 더 크고(경기 수 차 1당 170×4명, fair
  최대 5600) ② **카운터는 경기 종료 때만 오른다.** 4코트면 진행 4 + 대기 4 =
  **32석**이 완료 전에 미리 잡히므로, 한 라운드를 자기 자신에 대해 눈감고 짠다
- **손대지 않음**: 가중치·예약 시점 반영은 공정성과 맞바꾸는 설계 변경이라
  운영자 결정이 필요. 후보 지렛대는 (1) 대기표에 이미 잡힌 조합도 페널티에
  포함, (2) 파트너 1회 반복 페널티 240 → 상향

## 중복 예약 경고 (v1.10.555)

> 운영자: "마무리 후 자율게임 신청이 겹칠 때 어떤 의사결정을 내려야 할지
> 경고가 있어야 할 것 같아"

「다음 대진 짜기」에서 이미 예약된 선수를 고르면:

- 후보 줄에 **「N순위 예약됨」** 표시(`officialQueueIndexOfPlayer`)
- 확인창에 **⚠ 경고 블록** — 누가 몇 순위에 잡혀 있는지, 진행하면 그 팀은
  순번을 지킨 채 빈 자리가 자동 보충되고 **보충 인원이 없으면 해체**된다는 것까지
- **같은 네 명이 이미 잡혀 있으면** 별도 경고(중복 신청 가능성)

## 임원 권한 빈칸 메우기 (v1.10.554, **서버 배포 완료**)

운영자가 감사 결과 중 둘을 골랐습니다: "코트 번호 정정 추가해, 도착 되돌리기
처리, 나머지는 보류". 게임신청 수동 반영은 **회원 화면에 신청을 만드는 경로가
아예 없어서**(수락·거절만 가능) 뺐습니다 — 직접 요청은 「다음 대진 짜기」로.

- **`official-court-renumber`** — 진행 중 경기의 코트 번호 정정. 대상 번호가
  비어 있으면 이동, 차 있으면 **확인 후 맞바꿈**(`allowSwap`). 임원 화면에서는
  코트 배지를 눌러 실행(임원에게만 버튼, 회원에게는 그대로 표시). 경합은
  `expectedCourt` 지문으로 거름
- **`official-player-unarrive`** — 잘못 참가 등록된 선수를 도착 전으로 되돌림.
  **0경기 + 경기중 아님**만 허용(뛴 사람은 「경기 후 종료」가 맞는 처리).
  게임신청·대기표를 제외와 같은 경로로 정리한 뒤 planned. 「제외」 도구 모드의
  행에 「도착 전」 버튼으로 붙음 — 안 뛴 사람에게만 노출
- 관리자 원본의 검증 목록·재생(`_dailyApplyAdminOperation`)에 둘 다 배선.
  코트 맞바꿈은 재생 시 **상대 경기 번호까지 되돌려야** 두 경기가 같은 번호에
  서지 않습니다
- **보류**: 세션 전체 일시정지·재개(임원은 여전히 상태만 봄)

## 임원 화면 간소화 + 이름 키우기 (v1.10.546~553)

> 운영자 2026-08-13: "컬러 사용이 많아서 조잡해 보여, 점선박스 제거, 위로/
> 아래로 등 5개 버튼은 아이콘으로, 대진 당 상하로 길어지지 않게" ·
> "진행 중 코트 선수 이름 좀 더 키워서 — 가장 중요한 정보야" · "다음 대진도"

- **대진 카드 5버튼 → 1버튼 + 아이콘 줄**: 이름을 다는 건 자주 쓰는 「입장
  처리」 계열뿐. 위로·아래로·다시 짜기·삭제는 32px 아이콘 한 줄(우측 정렬)로
  접었다. 카드 액션 높이 **~230px → 82px**. 뜻은 title·aria-label 로 남긴다
- **(547) 일시정지·재시작도 아이콘 줄로** — 운영자 "일시정지도 4개 아이콘
  위치로 내리면 더 심플": 아이콘 줄 맨 앞에 ⏸/▶(재시작만 파랑 tint). 이제
  이름 붙은 버튼은 **「입장 처리」 하나**뿐이고, 그것도 없는 카드는 액션이
  **32px 한 줄**로 끝난다. 일시정지 상태는 카드의 「조금 쉬고 입장」 글자가
  말하므로 버튼에서 글자를 빼도 상태가 사라지지 않는다
- **(548) 선수 추가·코트 수·마무리를 운영 현황 도구 줄로** — 운영자 "운영현황
  버튼으로 처리": 패널 아래 상시 노출되던 **선수 추가 폼**·「코트 수 변경」·
  「마무리 전환」 세 덩어리를 지웠다. 도구 줄에 「선수 추가」(모드 — 켰을 때만
  폼이 펼쳐짐)·「코트 N」(즉시 prompt)·「마무리/마무리 해제」(즉시 토글)로
  들어간다. 도구가 7개가 되면서 `flex:1` 이 좁은 폰에서 「파트너 지정」을
  잘라먹어(실측) **wrap 2줄 + 내용 폭**으로 바꿨다
- **색 줄이기·점선 제거**: 상태는 테두리 한 겹으로만. 점선 박스(예정 행·
  안내 상자·교체 가능 이름·라이브 추가)를 실선 헤어라인으로 바꾸고 배경
  틴트를 걷어냈다. 삭제만 붉은 글리프로 남긴다
- **이름 위계**: 진행 중 코트 이름 16→**23px/1000**, 다음 대진 15→**19px/1000**
  (좁은 화면 21/17px). 잘림 방지로 줄바꿈 허용·자간 축소. 375px 실측에서 네
  이름 모두 안 잘림. 회귀가 크기와 위계(진행 중 > 다음 대진)를 고정
- **(549) `.replaceable` 함정 — v546 확대는 실제로 안 먹혔다**: 이름은 교체
  버튼(`.event-active-player.replaceable`)으로도 그려지고, 그 규칙의
  **`font:inherit` 가 크기·굵기를 되돌린다.** 더 구체적인 선택자라 `.event-
  active-player{font-size:…}` 만으로는 안 이긴다. 리그에서 span 으로 재현해
  20px 을 봤지만 실기기는 16px 이었다 — **실제 마크업(버튼)으로 재현해야
  한다.** 이제 두 선택자를 함께 걸고, 회귀가 `.replaceable` 포함을 요구한다
- **(550) 파트너 지정 은퇴 · 도구를 버튼형·중요도 순으로** — 운영자 "파트너
  지정은 다음 대진 짜기가 있어서 이젠 필요 없다 · 나머지를 버튼 형태로 중요도
  순 정렬". 순서: **선수 추가 → 마무리 → 코트 N → 운영 도우미 → 이름 변경 →
  제외**(자주 쓰는 순, 제외는 붉은 글자로 맨 뒤). 회색 판 위 글자 → **흰 버튼 +
  헤어라인 + 그림자**, 켜진 도구만 파랑으로 채움. 파트너 **모드 코드는 남김**
  (회원 게임신청 접수가 같은 경로를 씀) — 도구 버튼만 제거. 회귀가 순서와
  파트너 버튼 부재를 고정
- **(550) 「조금 쉬고」 알약 제거** — 운영자 "일시정지 표시가 있으니 필요 없다,
  오히려 그게 뭔가 해서 누르게 된다". 일시정지 카드에 상태 문구(`restPassText`
  = '일시정지')가 이미 뜨는데 큰 초록 알약이 버튼처럼 보였다. 상태는 문구가,
  푸는 길은 아이콘 줄 ▶ 재시작이 맡는다. `canRest` 는 restPass 가 아닐 때만
  참이라 회원의 「조금 쉬고」 신청 버튼과는 겹치지 않는다
- **(551~552) 이모지·글자 확대·상태 인라인** — 도구 버튼에 이모지(🙋 선수 추가 ·
  🏁 마무리 · 🏸 코트 N · 🤝 운영 도우미 · ✏️ 이름 변경 · 🚫 제외, `aria-hidden`),
  글자 12.5→**14.5px**. **함정 재발**: 전역 `button{flex-direction:column}` 때문에
  이모지가 글자 **윗줄로 올라갔다** — HANDOFF 에 이미 적힌 함정인데 또 밟았다.
  `flex-direction:row` 명시 + 회귀 고정. 「일시정지」 문구는 아이콘 줄 왼쪽으로
  합쳐 제 줄이 사라졌다(`event-next-actionbar`)
- **(552) 직접 짠 대진이 목록에서 사라지던 버그** — 운영자 "다음 대진 짜기
  예약하면 리스트업 해줘. 그래야 알 거 아냐". 원인: 게시가
  `_dailyQueue.slice(0,cap.target)` 로 잘랐고, 수동 편성은 **목표 수 밖에
  붙으므로 통째로 안 실렸다.** 임원 화면 `eventNextList()` 도 코트 수로 또
  잘랐다. 이제 **자동만 목표 수 제한, 수동은 항상 통과**(양쪽 모두).
  `manualComposed` 를 payload 에 싣고 **「직접 예약」 딱지**를 다음 대진 카드와
  편성 시트 예약현황 양쪽에 붙였다. 코트 3 + 수동 1 시나리오를 실행해 고정
- **(553) 쓰임새로 묶어 3열 두 줄 고정** — 운영자 "사용자 관점에서 봐야 해":
  윗줄 **명단 셋**(🙋 선수 추가 · ✏️ 이름 변경 · 🚫 제외), 아랫줄 **운영 설정
  셋**(🏸 코트 N · 🤝 도우미 N · 🏁 마무리). 감싸기(wrap)에 맡기면 라벨 길이에
  따라 묶음이 흐트러져 **3열 그리드로 고정**. 104px 칸에 「운영 도우미 N」이
  잘려(실측) **「도우미 N」**으로 줄임 — 가장 넓은 조합(코트 12 · 도우미 2 ·
  마무리 해제)까지 잘림 없음 확인
- **(553) 「예약」 딱지는 임원 화면만** — 운영자 "일반 사용자 화면에는 필요
  없어" · "직접 예약 → 예약으로 간소화". 회원에게는 운영 사정일 뿐이라
  `canOfficialOperate` 로 가림
- **리그 측정 주의**: 브라우저 패널이 숨겨져 있으면 `window.innerWidth` 가 0 이
  되어 **모든 폭 측정이 0**으로 나온다(2026-08-13). 폭을 잴 때는 먼저
  `resize_window` 로 뷰포트를 세우고, 측정용 노드는 `main.wrap` 같은 실제 폭
  있는 컨테이너에 붙일 것 — body 직속은 0 이 나온다
- **(549) 「진행중」 글자 제거**: 남은 시간 배지·코트 번호·붉은 카드가 이미
  말한다. 배지(파트너·보정)가 있을 때만 머리줄을 남겨 빈 줄이 안 생긴다

## 명부 오클럽의 진짜 원인 (v1.10.545) — 임원 화면이 순서를 버렸다

> 운영자 2026-08-13: "여전히 미르클럽으로만 나옴. 안되는 거면 안된다고 해."

**533~543 다섯 판을 관리자 쪽만 고쳤는데 다 헛수고였다.** 원인은 임원 화면:

```js
officialArrivalPlayers(){ … return [...existing,...roster].sort(이름순); }
officialPrimaryClub(){ return officialArrivalPlayers().find(kind==='roster').club; }
```

관리자가 오늘 클럽을 **맨 앞에 두어** 후보를 실어 보내면, 임원 화면이 그 배열을
**이름순으로 정렬**한 뒤 **0번 항목의 클럽**을 "오늘 클럽"으로 읽었다. 즉
**가나다 순으로 앞선 이름이 속한 클럽**이 오늘 클럽이 됐다. 미르 회원 이름이
앞서면 영원히 미르다 — 관리자가 무엇을 해도 뒤집힌다.

**수리(간단하게):** 순서로 추측하지 않는다. 관리자가 `session/arrivalClub` 에
**클럽 이름을 그대로** 실어 보내고(전체 게시 + 부분 동기화 양쪽), 임원 화면은
그 필드를 읽는다. 필드가 없는 옛 게시는 **정렬 전 원본 배열**의 첫 명부 후보를
본다(정렬된 목록이 아니라).

**교훈:** 기존 회귀는 `/primaryClub/.test(src)` 처럼 **배선만** 확인해서 전부
통과했다. 이제 실제로 실행해 결과를 고정한다 — 이름순 첫 후보가 미르인 상황을
만들어 놓고 `officialPrimaryClub()==='일만클럽'` 을 단언한다.

**현재 세션 안내:** 기록(`sessionClub`)이 없는 세션은 여전히 겹침 추정이
`arrivalClub` 을 채우므로, 관리자 새로고침 → 「오늘 클럽 확인」 알림에서
일만클럽 → 임원 화면 새로고침. (v543 의 그 버튼은 옳았지만 임원 화면이
결과를 버려서 효과가 없었다.)

## 복구 배너 닫기 + 오늘 클럽 지정 (v1.10.543) — 라이브 중 실전 2건

1. **복구 배너 「복구 안 함 · 닫기」** — 운영자: "오늘 명단 임원 아니면 자격
   복구 필요 없어. 닫는 기능도 있어야". 명부 임원인데 오늘 임원으로 안 뛰는
   선수는 복구 대상이 아니다. 닫으면 그 이름은 `_dailyOfficialRestoreDismissed`
   (저장·복원·초기화 동행)에 접혀 다시 안 뜨고, **새 이름이 감지되면 그
   이름으로만** 다시 뜬다. 표시·복구 실행·닫기 모두 접은 이름을 뺀
   `_dailyOfficialRestoreCandidates()`를 쓴다(복구 버튼이 접은 사람까지
   복구하는 사고 방지)
2. **「오늘 클럽 확인」 알림** — 운영자: "임원화면 명부 여전히 미르클럽만".
   원인: 기록 도입(537) 전에 만든 라이브 세션은 `sessionClub`이 비어 있어 겹침
   추정으로 굴러간다. 추정을 더 쌓는 대신, **기록 없는 라이브 + 클럽 여럿**이면
   상황판 알림에 클럽 버튼이 떠서 관리자가 한 번에 확정(`dailySetSessionClub`:
   기록 + dailySave + 후보 부분 동기화 + 알림 소멸). importDailySelected 의
   최초 기록도 라이브 중이면 즉시 부분 동기화하도록 보강. **운영자 안내: 관리자
   새로고침 → 알림에서 일만클럽 탭 → 임원 화면 새로고침**

## 임원 화면 버튼 위계 (v1.10.544)

> 운영자: "각 버튼을 중요도에 따라 간결하고 애플스런 디자인으로. 이미지 버튼을
> 잘 활용해봐"

**네 단계 위계** — 문맥마다 채운 파랑 주버튼 하나만:

| 단계 | 모양 | 버튼 |
|---|---|---|
| 주 | 채운 파랑 | 경기 종료 · 입장 처리 · 지각 참가 등록 |
| 보조 | 파랑 tint | 다음 대진 짜기 |
| 조용 | 흰 바탕 + 헤어라인 | 다시 짜기 · 이번만 뒤로 · 마무리 취소 |
| 파괴 | 붉은 tint(채우지 않음) | 경기 취소 · 대진 삭제 |

- **아이콘**: `obIcon()` 인라인 SVG 한 벌(check·x·enter·redo·trash·plus·copy).
  외부 자산 0 — 오프라인·저속 회선에서 안 깨집니다. `currentColor` 라 버튼
  위계 색을 그대로 입습니다
- 운영 도구 4모드는 **세그먼트 컨트롤**(회색 트랙 + 흰 알약), 타일·상자는
  헤어라인 카드로 통일, 탭 시 `scale(.965)`
- **전역 button 규칙 두 개가 새어 들어온 것을 잡음**: (1) `flex-direction:column`
  이 아이콘을 글자 위로 올림 → 아이콘 버튼만 row 명시, (2) `button.active` 의
  초록 그라디언트+흰 글자가 고른 타일 라벨을 배경에 묻히게 함 → 타일이 자기
  색을 끝까지 정함
- **삽입 위치가 곧 우선순위**: 처음엔 블록을 스타일 앞쪽에 넣어 원본 규칙에
  전부 덮였습니다(리그 스크린샷으로 발견). 블록은 `</style>` 직전에 있어야 하고,
  회귀가 이 순서를 고정합니다

## 수동 편성 전면 교착 수리 (v1.10.527) — 세션 직전 시뮬레이션이 잡음

28명·3코트·180분 사전 시뮬레이션에서 **180분에 11경기, 5파동부터 전 코트
정지**가 재현됐다. 원인 둘: ①`startPreparedItem` 이 품질을 재검사해 실력차 큰
수동 편성이 자동·수동 입장 모두 조용히 실패 + 대기표 자리를 차지해 보충 정지
②준비 안 된 편성(경기중 선수 대기)이 빈 코트 cue 를 붙잡으면 그 코트는 자동
투입 경로가 없어 사람이 눌러야만 살아남. 수리 후 같은 시뮬레이션 36경기,
전 파동 3코트 가동, 인당 4~6판. `daily-queue-compose-regression.js` 가 고정.
시뮬레이터는 스크래치패드 `sim28.js`(임시) — 필요하면 tests/ 로 승격할 것.

## 권한 설계 원칙 (운영자 확정 2026-08-10) — 앞으로의 게이트 판단 기준

> "임원의 게임설정 자유권을 최대로 보장하고 우리 시스템은 그 이후의 상황에
> 대해 안정적이고 밸런스있는 게임지원을 계속하는 방식"

설정(선수 구성·교체·경기 생성·순서)은 임원에게 열고, 시스템의 역할은 사후 보정
(공정성 기대치, 자동 보충, 지문 검증으로 경합만 거름)이다. 이 원칙으로 이날
서버 관문 셋이 전부 열렸다: ①`official-queue-replace` 의 `inPlayerId` 지정
②`official-manual-match` ③`allowFreeMove`(순서 임의 이동). 운영 명령에서
관리자 전용으로 남은 것은 명단·설정 계열(선수 추가/제외/이름, 코트 수, 마무리,
경기 취소, 대기 삭제/재생성, 게임신청 반영)뿐이다.

## 다음 대진 교체 + 새 게임 등록 (v1.10.525)

- **다음 대진 카드의 선수 이름도 버튼** — 진행 코트와 같은 시트로 지정 교체.
  일시정지 요구 폐지(경합은 서버 지문이 거름), 게임신청으로 잡힌 대진은
  이름을 버튼으로 만들지 않습니다. 옛 "서버가 골라주는" 교체 UI는 제거.
- v1.10.525 의 「새 게임 등록」(코트 즉시 시작)은 반나절 만에 v1.10.526 에서
  「다음 대진 짜기」로 대체됐습니다 — 운영자가 의미를 정정했습니다.

## 다음 대진 짜기 · 순서 변경 · 후보 무제한 (v1.10.526, 서버 배포 완료)

운영자 정정 셋:

> "새 게임 등록은 **다음 대진을 수동으로 짠다**는 뜻" · "**순서 변경**도 임원에게" ·
> "교체 후보에 **제한을 두지 말고**, 경기 중인 선수를 고르면 **종료까지 투입 보류** + 딱지"

만든 것:

- **`official-queue-add` 신설** — 4명을 골라 대기표 끝에 붙임(`manualComposed`).
  버튼은 「＋ 다음 대진 짜기」. 선택한 선수가 걸린 기존 대기 경기는 통째 해체
  (수동 경기 등록과 같은 방식 — 한 명씩 repair 는 이중 배치를 만듦, 회귀로 고정)
- **순서 변경** — 대기 카드에 ▲위로/▼아래로, `allowFreeMove` 임원 개방(마지막
  관리자 전용 게이트 소멸). 구성 불변·되돌리기 쉬움이라 확인창 없음
- **후보 무제한** — 교체·편성 후보에 경기 중 선수 포함(붉은 「N코트 경기중」 딱지,
  뒤로 정렬). 휴식·종료·도착 전만 제외(그 상태가 들어간 대진은 서버가 지움)

**함정 셋을 회귀로 고정**(`daily-queue-compose-regression.js`):

1. **품질 필터의 조용한 삭제** — 실력 차 큰 수동 편성을 `removeInvalidPrepared` 와
   입장 검사가 지우거나 막던 것. `manualComposed` 면제
2. **동기화 잘라내기** — 관리자 원본의 `_dailyApplyServerQueueSync` 가 목표 수로
   잘라 끝에 붙인 수동 대진을 버리고, 균형 검사로 동기화 자체를 실패시키던 것.
   수동 편성은 제한 밖 + 보관 판정 완화
3. **이중 코트** — 경기 중 선수가 든 대진의 투입. 서버 `queueReady` + 관리자
   `_dailyQueueItemStartable`(보관/시작 판정 분리)이 막고, 입장 거절 사유가
   "○○ 선수가 경기 중입니다"로 정확히 나옴. 카드에도 호박색 딱지
   ("○○ 경기중 — 경기 종료 후 투입")

실측: 편성 시트(A팀/B팀 배지·경기중 딱지·확인창 보류 안내) · 교체 시트(경기중
그룹) · 순서 이동 payload · 카드 딱지까지 브라우저 실행으로 확인. 테스트 64개
통과, functions 배포 완료. **경기 중 선수를 넣은 편성이 실제 서버 왕복에서
종료→자동 투입으로 이어지는 흐름은 화면 실측 전**입니다.

## 교체 시트 디자인 수리 (v1.10.524) — 실기기가 잡아낸 것

운영자 실기기 스크린샷에서 **메타 텍스트("0G · 대기")가 카드 밖으로 밀려
잘렸습니다.** 원인: checkin.html 859행의 전역 `button` 규칙이
`flex-direction:column` 을 깔아 두는데, 시트 행이 방향을 명시하지 않아
이름·메타가 세로로 쌓이며 넘쳤습니다. **로컬 리그에서는 그럴듯하게 보여서
못 잡았고, 실기기 스크린샷이 잡았습니다.**

고친 디자인(운영자: "예쁘고 심플하게"): 이름 왼쪽·메타 오른쪽 **한 줄 행**(54px),
그룹 제목은 스크롤에 고정(sticky), 색은 셋만 — 보통 회색 `0G` / 다음 대진 예정
호박색 / 맞교환 붉은 카드. 그룹 제목도 「대기 선수」「다른 코트 경기중 · 맞교환」
으로 줄였습니다(휴식 전환 안내는 확인창이 이미 말합니다).

교훈: **이 파일의 전역 `button` 은 세로 flex 입니다.** checkin.html 에 새 버튼
컴포넌트를 만들면 `flex-direction` 을 반드시 명시하십시오.

## 임원 선수 교체 — prompt 폐지, 이름 탭 → 후보 시트 (v1.10.523)

> 운영자: "해당 선수 이름을 누르면 선수교체 창이 뜨고 선수명단이 뜨고 골라
> 바꾸면 되는 방식으로"

임원 화면(checkin.html) 진행 코트 카드의 **선수 이름이 버튼**이 됐습니다
(노란 점선 — 일시정지 대진의 이름 교체와 같은 시각 언어). 누르면 그 선수를
빼는 **후보 시트**(complete-picker 패턴 재사용)가 뜨고, 후보를 누르면 확인 후
`official-active-replace` 를 보냅니다. 「선수 교체」 버튼과 번호 입력 prompt 는
폐지. 후보는 「대기 선수(휴식 전환)」와 「다른 코트 경기중(맞교환, 붉은 행)」
두 그룹으로 나뉘고, 예약 선수는 `대기(다음 대진 예정)` 표시로 뒤에 옵니다.

같이 나간 것: 참가자 모달의 「전체 선택/해제」를 **목록 머리글로 이동**
(운영자: "메뉴가 하단에 있어서 헷갈려"). 하단 줄에는 등록 버튼 둘만 남음.

실측: 시트 열기 → 대기 투입 선택(요청 payload 캡처 확인) → 맞교환 선택(⚠️ 경고
확인) → 시트 닫힘, 모바일 375px 스크린샷까지. 회귀 8b가 prompt 복귀·옛 버튼
부활·후보 구분 소실을 잡습니다. 관리자 화면(index.html)은 여전히 prompt +
이름 입력입니다 — 임원만 요청받았고, 관리자 화면은 이름 탭이 이미
`dailyPickActiveReplacement` 로 배선돼 있어 다음에 같은 시트로 바꾸면 됩니다.

## 오등록 삭제 — 이미 있음 (새로 만들지 않았다)

운영자가 "참가자 등록 시 오등록한 경우 삭제 기능"을 요청했는데, **셋 다 이미
있습니다.** 답만 하고 코드는 안 건드렸습니다.

| 상황 | 있는 곳 |
|---|---|
| 명단에서 아예 제외 | 선수 목록 위 도구 「삭제」 → 행의 빨간 「삭제」 (게시 후엔 `official-player-remove`) |
| 잘못 도착 처리한 선수 되돌리기 | 0경기 선수 행의 「도착 전」 버튼 (daily.js 3291 "잘못 등록한 경우의 정정") |
| 게시 후 라이브 추가 오등록 | 상황판 「라이브 후 추가」의 「오등록 취소」 |

## 새로 만든 것 — 참가 등록 통합 (2026-08-09 밤, v1.10.522)

운영자 요구 둘:

> "현장 참가 등록과 게스트 등록 함께 하고 싶은데 따로 처리되는 것 같아"
> "미리 도착 전으로 등록하고 현장에서 한번에 현장 참가로, 일부만 도착 전으로"

일괄 도착 기능 자체는 이미 있었습니다(모달 재진입 → 선택 → 현장 참가 등록).
문제는 셋이었고 전부 화면 쪽이었습니다 — 서버 `official-player-create` 는
`status:'planned'`·`isGuest` 를 처음부터 받고 있었습니다.

| 문제 | 조치 |
|---|---|
| 게스트는 모달에 못 들어가 폼에서 한 명씩 | 모달 안에 게스트 추가 폼(도착 전·현장 참가 버튼 각각) |
| 게스트를 도착 전으로 등록할 방법이 없음 | 생성 코어 `_dailyCreatePlayerDirect` 에 status 전달 |
| 「전체 선택」이 안 오는 명부 회원까지 쓸어 담음 | 「보기: 전체 / 도착 전」 필터. 전체 선택은 보이는 행만 집으므로 필터가 곧 선택 범위 |

구조 변화 둘:

- **명부 밖 도착 전 선수(게스트)가 모달 목록에 뜹니다**(`p:<id>` 행).
  일괄 도착에 게스트가 포함되려면 목록에 있어야 합니다. 이 행은 프로필을
  다시 쓰지 않고 상태만 바꿉니다(`_playerRow`) — 게스트 표시·급수가 유지됩니다
- **선수 생성이 한 벌로 모였습니다.** 「선수 추가」 폼과 모달 게스트 추가가
  같은 `_dailyCreatePlayerDirect` 를 씁니다. 두 벌로 복사하면 한쪽만 고치는
  사고가 납니다

검증: 회귀 `tests/daily-import-bulk-arrival-regression.js`(게스트 생애 전체를
엔진에 실제로 돌림 + 배선 검사) + **브라우저 실측**(정적 사본 서빙, 데스크톱·
모바일 375px). 모달 렌더·필터·전체 선택 범위·일괄 도착·게스트 폼 추가까지
실행으로 확인했고, 모바일에서 게스트 이름칸이 셀렉트에 눌려 안 보이던 것을
현장에서 잡아 고쳤습니다(319px 전폭으로 확인).

**남은 것**: "일부만 도착 전으로 되돌리기"는 모달이 아니라 기존 「라이브 후
추가」의 오등록 취소 경로가 담당합니다(README: 아직 경기하지 않은 오등록만
취소하거나 도착 전으로 되돌림). 게시 후 서버 명령 경로의 화면 실측은 아직입니다
— 이번 실측은 게시 전 로컬 경로였습니다.

## 가장 중요한 것 — 교체는 배포해도 동작하지 않았습니다

「진행 중 게임의 선수를 교환할 수 있나」를 확인하다 찾았습니다. **배포만으로는
안 됐습니다.** 관리자 원본의 검증 함수(`_dailyOfficialRequestError`)에
`official-active-replace` 분기가 **없었습니다.** 분기 자체는 쓰여 있었지만
검증 함수가 아니라 **적용 함수 안에 붙어 있었고**, 그 자리에서는 바로 위
같은 조건의 분기가 이미 `return` 해서 **한 번도 실행되지 않는 죽은 코드**였습니다.

결과는 교체 한 건이 실패하는 정도가 아닙니다. 거절된 순간
`serverReconcileBlocked` 가 켜져 **그 뒤 모든 서버 결과가 관리자 화면에 안
들어옵니다.** 5단계가 없애려는 그 리비전 함정과 같은 증상입니다.

**같은 이유로 셋이 더 막혀 있었습니다** — `official-queue-replace`(대진 선수
변경) · `official-queue-hold` · `official-queue-resume`. 셋 다 임원 화면이
실제로 보내는 명령이고, 적용 분기도 멀쩡히 있는데 검증에서 먼저 떨어졌습니다.
이 셋은 fb32b47 이전부터 그랬습니다. HANDOFF 의 「실측 미완료 — 대진 선수/순서
변경」이 이것과 맞물립니다.

넷 다 검증 함수에 분기를 넣어 고쳤고, 고치기 전 코드에서 검사기가 넷을 이름까지
짚어 실패하는 것을 확인했습니다.

## 0. 테스트 — 63개 전부 통과

**`node` 는 PATH 에 없습니다.** 시스템에 설치돼 있지 않고, 코덱스 런타임에 딸린
것을 씁니다. 경로를 모르면 "Node 가 없다"고 잘못 판단하게 됩니다.

```bash
cd "/Users/gimminhyeon/Documents/Codex/2026-06-09/new-chat-2/badminton" && NODE=~/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node && for f in tests/*.js; do $NODE "$f" >/dev/null || echo "FAIL $f"; done; echo done
```

이번 변경 기준 63개 전부 통과했습니다(v24.14.0). 새로 나오는 줄:

```
js/daily.js 최상위 함수 785개, 중복 정의 없음
후보 목록: 상한 없음 · 다음 대진 예약 선수도 후보(뒤로 정렬)
번호·이름 선택 11가지: 관리자·임원 판정 일치
관리자 원본 검증: 서버 적용분 수용 · 게시 전 전제 검사 유지
관리자 원본이 아는 서버 명령 28종 확인
```

마지막 줄이 이번에 넣은 핵심 검사입니다. **서버가 받는 명령 28종을 관리자
원본이 전부 아는지** 대조합니다. 종전에는 '관리자 전용' 10종만 봤고, 임원도
쓰는 명령이 빠지는 것을 못 잡았습니다. **새 명령을 만들면 서버·임원 화면·관리자
검증·관리자 적용 네 곳을 함께 손대야 합니다.**

## 1. 운영자가 해야 할 것 — 서버 배포 (그대로 미결)

```bash
cd "/Users/gimminhyeon/Documents/Codex/2026-06-09/new-chat-2/badminton" && firebase deploy --only functions
```

**2026-08-09 실행 완료.** 그런데 결과가 예상과 달랐습니다 — 함수 5개 전부
`Skipped (No changes detected)`. **서버는 이미 최신이었습니다.** 어제 HANDOFF 의
"미배포" 는 사실이 아니었고, 경기 취소 후 자동 투입 제외도 이미 올라가 있었습니다.

즉 **교체가 안 되던 원인은 서버가 아니라 관리자 화면이었습니다**(위 「가장
중요한 것」). 그 수정은 커밋·푸시했으므로 GitHub Pages 반영 후 동작합니다.

## 2. 선수 교체 — 운영자 답이 나왔습니다 (2026-08-09)

> "코트 간 맞교환이라기보다, 원하는 선수로 교환하려다 보면 대기선수일 수도 있고
> 다른 코트 선수일 수도 있어 이 두 가지 경우에 대응할 수 있어야 해"

즉 **'맞교환'은 별도 기능이 아니라 한 기능의 두 경우**입니다. 운영자는 "이 선수로"
를 지목하고, 그 선수가 대기 중이냐 뛰는 중이냐에 따라 처리가 갈릴 뿐입니다.
맞교환은 목록에서 빼지 않습니다.

그 기준으로 보니 **지목한 선수가 목록에 아예 안 뜨는 경우가 둘 있었습니다.**

| 증상 | 원인 | 조치 |
|---|---|---|
| 대기 선수가 11번째부터 안 보임 | 관리자 화면이 후보를 `.slice(0,10)` 으로 잘랐습니다 | 상한 제거 |
| 다음 대진에 예약된 대기 선수가 안 보임 | 후보 계산이 큐에 든 선수를 목록에서 **지웠습니다** | 지우지 않고 뒤로만 정렬, `대기(다음 대진 예정)` 로 표시 |

둘 다 관리자 화면만의 제약이었고 **임원 화면(checkin.html)에는 없었습니다.**
서버 `applyActiveReplace` 도 예약된 대기 선수를 받아들입니다(`repairPreparedForUnavailablePlayer`).
즉 관리자 화면 혼자 서버보다 좁았습니다.

목록 상한을 없애면 스무 줄을 넘길 수 있어 **번호 대신 이름으로도 고르게** 했습니다.
공백은 무시하고, 동명이인이거나 여러 명에 걸리면 번호로 고르라고 안내합니다.
두 화면이 같은 규칙이며(`_dailyPickFromCandidates` · `officialPickFromCandidates`),
회귀가 두 함수의 판정을 **직접 대조**합니다.

### 아직 없는 것 — 같은 코트 안에서 팀만 바꾸기

`가·다 vs 나·라` → `가·나 vs 다·라` 처럼 같은 4명으로 편만 재구성하는 것입니다.
운영자가 이 건은 언급하지 않았습니다. 만들려면 작습니다 — 서버의 `swapIds`
헬퍼가 이미 두 자리를 맞바꾸므로, 같은 경기·반대 편일 때의 거절
(`daily-official-engine.js` 의 `이미 이 경기에서 뛰는 선수입니다`)만 풀면 됩니다.
선수 상태·타이머·경기 수는 건드릴 일이 없습니다.

## 3. 이번에 같이 치운 것 — 161줄 중복 블록

`js/daily.js` 에 함수 6개가 **두 벌씩** 정의돼 있었습니다
(`_dailyActiveReplacementCandidates` · `_dailyFindQueueReplacement` ·
`_dailyRemoveQueuedPlayer` · `_dailyTryReplaceQueuedPlayer` ·
`dailyEditQueuePlayer` · `dailyPickQueueReplacement`). `fb32b47` 에서 새 교체
함수를 끼워 넣을 때 앞 블록을 복사해 두고 지우지 않은 것입니다.

두 벌이 한 글자도 다르지 않아 **동작은 멀쩡했습니다.** 그래도 지웠습니다 —
앞쪽을 고치면 뒤쪽이 조용히 덮는, 8일 밤에 한 시간을 버린 바로 그 덫입니다.

**어제 넣은 검사는 이걸 못 잡았습니다.** `dailyPickActiveReplacement` 이름
하나만 세고 있었기 때문입니다. 이름을 지정하지 않고 **파일의 최상위 함수 전체**
를 세도록 바꿨습니다(회귀 6번). 어제 파일로 돌려 6개를 이름까지 짚어 실패하는
것을 확인했습니다.

## 4. 남은 큰 것 — 5단계 (근본 원인)

**8일 밤 실제로 재발했습니다.** 서버 rev 21, 관리자 화면은 낮 12시에 멈춤,
명령 기록은 4건만 남아 복구 불가.

원인: 관리자 화면이 세션을 **통째로 게시**하는데, 그 게시는 서버 리비전을 순서대로
따라잡아야 합니다. 명령 기록은 60분만 보관되므로 한 시간 이상 어긋나면 영원히
복구되지 않습니다.

응급 처치로 `dailyAdoptServerState()`(상황판 상단 「서버 기준으로 다시 맞추기」
배너)를 만들었지만, **덫 자체는 그대로입니다.** 5단계
(`_dailyWriteCheckinPayload` 제거 = 관리자 직접 쓰기 폐지)를 해야 재발이 없습니다.

순서는 `MINTON_LIVE_ADMIN_WRITE_UNIFICATION.md` 의 「5단계를 어떻게 할 것인가」
그대로입니다. **라이브 한 판 실측이 먼저입니다 — 코드로는 더 못 나갑니다.**

## 5. 그 밖에 미결

- **`database.rules.json`** — 조인 규칙을 만들어 뒀으나 배포 보류(운영자 판단).
  `firebase deploy --only database` 로 적용. 반쪽짜리라 급하지 않음
- **실측 미완료** — 도착 전 등록(모달), 수동 경기 등록, 다음 대진 투입,
  대진 선수/순서 변경. 이번에 고친 **교체 후보 목록도 화면 실측 전**입니다.
  테스트 62개는 통과했지만, 8일에 한 시간을 버린 그 결함도 테스트는 통과했습니다
- **교체 선택창이 아직 `prompt()`** 입니다. 이름 입력으로 급한 불은 껐지만,
  후보가 서른 명이면 모달이 맞습니다. 실측하고 판단하십시오

## 배운 것 (반복하지 말 것)

- **화면에서 실제로 실행해 확인할 것.** 8일에 만든 교체 기능은 같은 이름의 옛
  함수가 파일 뒤쪽에 남아 새 함수를 덮고 있어 **한 시간 동안 실행조차 안 됐습니다.**
  단위 테스트는 통과했습니다
- **감사에서 지적한 것은 검사기 규칙으로 옮길 것.** 이름 하나만 막은 검사는
  바로 다음 중복을 통과시켰습니다. 규칙을 넣었으면 **낡은 파일로 실패를 확인**
  하십시오. 실패를 안 보면 검사기가 도는지 알 수 없습니다
- **후보 목록에서 지우지 말 것.** 정렬로 뒤에 두는 것과 목록에서 없애는 것은
  다릅니다. 운영자가 지목한 선수가 없으면 그 작업 자체를 못 합니다
- **"만들었다"와 "닿는다"는 다릅니다.** 교체 검증 분기는 코드로 존재했지만
  엉뚱한 함수 안에 있어 한 번도 실행되지 않았습니다. 중복 정의도, 이것도,
  붙여넣기가 자리를 틀린 것입니다. **새 분기를 넣었으면 그 분기가 실제로
  불리는지 호출해서 확인하십시오** — 소스에 문자열이 있는지 보는 검사는
  이 둘을 구별하지 못합니다
- **서버·클라이언트에 같은 검사가 두 벌이면 한쪽만 고치지 말 것.** 이번에도
  관리자 화면만 서버보다 좁았습니다
- **실기기 확인은 로컬 HTTPS 리그 말고 실제 배포로**
