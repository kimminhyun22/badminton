# 콕매치 디자인 개선 브리프 (코덱스 작업 지시) — 2026-09-02

## 목표
배드민턴이라는 **역동적인 스포츠**의 이미지에 맞게, **간결하지만 힘 있는** 시각 언어로
전체 디자인·버튼·글꼴을 다듬는다. 기능·흐름·문구는 그대로 두고 **보이는 것만** 바꾼다.

## 대상 화면 (우선순위 순)
1. `checkin.html` — 회원·임원이 코트 옆에서 폰으로 보는 화면. 가장 많이 쓰인다. (CSS·JS 인라인)
2. `index.html` + `css/app.css` — 관리자 민턴LIVE 상황판.
3. `team.html` + `css/team.css` — 팀전 관리자. `rsvp.html`, `view.html`(체육관 TV 중계), `quiz.html`.

## 디자인 방향
- **정체성**: 셔틀콕의 속도감·코트 라인의 기하학. 배경은 밝고 깨끗하게, 강조색은 절제된 2색
  (예: 코트 그린 계열 + 셔틀콕 화이트/네온 라임 한 점, 혹은 현재 브랜드의 레드 LIVE 포인트 유지).
  장식용 이미지·그라데이션 남용 금지. 카드 그림자·라운드는 지금보다 덜, 여백은 지금보다 넉넉히.
- **글꼴**: 이름·숫자가 3~5m 거리에서 읽혀야 한다(코트 옆 폰). 숫자는 tabular-nums.
  **모든 글꼴은 화면 폭에 따라 리사이징**되게 한다 — 루트 `font-size: clamp(...)` + rem 기반, 또는
  뷰포트 단위 clamp. **font-weight 는 900 상한**(로드된 Noto Sans KR 최대; 1000 은 일부 폰에서
  글리프가 깨진다). 긴 한글 이름은 `overflow-wrap:anywhere`로 절대 잘리지 않게. OS 글자 확대
  설정(120~150%)에서도 레이아웃이 깨지지 않아야 한다.
- **버튼**: 1차 행동 하나만 채움 버튼, 나머지는 윤곽/텍스트. 터치 높이 44px 이상. 위험 동작(초기화·
  삭제·종료)은 빨강 계열로 구분. 상태(진행·대기·휴식·종료·도착 전)는 색+텍스트 둘 다로.
- **다크 모드**: 삼성 인터넷 강제 다크에서 배지가 사라지지 않게 색 대비를 확인(prefers-color-scheme
  대응은 선택, 최소한 강제 반전에 견딜 것).

## 반드시 지킬 것 (깨지면 실패)
- **HTML id·class 훅, onclick 핸들러, data-* 속성, 텍스트 문구를 바꾸지 않는다.** 렌더 문자열(js/*.js,
  checkin.html 의 JS)이 만드는 클래스 이름도 유지. 새 클래스는 **추가**만.
- 엔진·서버(`functions/`)·데이터 로직·Firebase 경로 금지. 순수 CSS + 최소 마크업(래퍼/클래스 추가).
- 작업 후 반드시 `for f in tests/*.js; do node "$f" || echo FAIL $f; done` 를 돌려 **전부 통과**시킬 것.
  특히 `tests/mobile-layout-regression.js`·`tests/checkin-visual-hierarchy-regression.js`·
  `tests/checkin-field-feedback-regression.js` 는 CSS 문자열을 고정한다 — 그 규칙은 값이 아니라 뜻을
  지키되, 문자열이 바뀌면 테스트 의도를 확인하고 테스트도 함께 정직하게 갱신한다.
- 검수 기준은 **모바일 375px**(memory: 폰에서 축소되어 보인다). 320px 에서도 가로 스크롤 없음.
- 버전 문자열(app-version, ?v=)은 건드리지 않는다 — 배포 시 운영자가 올린다.

## 산출물
- 변경 파일 + 짧은 `DESIGN_NOTES.md`(무엇을 왜 바꿨는지, 색·글꼴 토큰 표, 검수 방법).
- 커밋은 이 브랜치(`codex/design-refresh`)에만. main 에는 절대 머지하지 않는다.
